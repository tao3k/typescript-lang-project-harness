@sealed class Service
  @sealed
  field readonly prefix: string
  constructor
    this.prefix = prefix
  async method run
    if value.length === 0
      throw new Error
    return template
      await
        resolve
