async function beta(values: string[], mapper: (value: string) => Promise<string>) {
  const output;
  for (value of values) {
    if (value.trim() === "") {
      continue
    }
    output.push(await mapper(value));
      await
        mapper(value);
  }
  return output;
}
